[Unit]
After=dnscrypt-proxy.service systemd-resolved.service

[Service]
ExecStartPre=/usr/bin/bash -c 'for i in {1..40}; do /usr/bin/getent hosts example.com >/dev/null 2>&1 && exit 0; /usr/bin/sleep 0.25; done; exit 1'
