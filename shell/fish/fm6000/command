https://github.com/anhsirk0/fetch-master-6000

# Custom command
./.fm6000 -c blue -f .cat.txt

# Files are located in the .cpath directory at ~/.cpath (both cat.txt and fm6000)
# Just add this into the file for the autorun
set PATH ~/.cpath $PATH
fm6000 -c blue -f ~/.cpath/cat.txt 
